---
status: pending
title: Ultra-minimal Hello World App
---

Current state: the project contains only `README.md`. Everything below is created from scratch.

1. Scaffold the Vite project files at the repo root: `package.json` (ESM, npm, scripts for dev/build/preview, deps: react, react-dom, @tanstack/react-router; devDeps: vite, @vitejs/plugin-react, typescript, @types/react, @types/react-dom, @tailwindcss/vite, @tanstack/router-plugin), plus `.gitignore` covering `node_modules` and `dist`. Outcome: `npm install` and `npm run dev` are runnable.

2. Create `index.html` at the repo root with a single `#root` mount node and a module script pointing at `/src/main.tsx`. Set the document title to "Hello World". Outcome: Vite has an entry HTML document.

3. Create `vite.config.ts` registering, in order, the TanStack Router plugin (`@tanstack/router-plugin/vite`, React target, routes dir `src/routes`, generated tree `src/routeTree.gen.ts`), the React plugin, and the Tailwind plugin (`@tailwindcss/vite`). Add a resolve alias mapping `@` to `src`. Outcome: file-based routing and Tailwind v4 are wired into the build.

4. Create `tsconfig.json` (and `tsconfig.node.json` if needed for the Vite config) with strict mode, `bundler` module resolution, JSX set to `react-jsx`, and the `@/*` → `src/*` path mapping matching step 3. Outcome: TypeScript resolves the alias and type-checks cleanly.

5. Create `src/styles/global.css` whose first line is exactly `@import "tailwindcss";` and nothing else. Outcome: Tailwind utilities are available app-wide.

6. Create `src/main.tsx`: import `./styles/global.css` once, import the generated route tree from `@/routeTree.gen`, create the router, register its types, and render `RouterProvider` into `#root` inside `StrictMode`. Outcome: the app boots through TanStack Router.

7. Create `src/routes/__root.tsx` exporting the root route with a minimal shell — a full-height wrapper with a neutral background (e.g. a light neutral tone) rendering `<Outlet />` and no chrome, header, or nav. Outcome: a clean, empty app shell.

8. Create `src/routes/index.tsx` for the `/` route: a full-height flex container centering content both vertically and horizontally, containing a single heading reading "Hello World" with restrained typography (large, medium/semibold weight, tight tracking, dark neutral text). No interactivity, no additional elements. Outcome: visiting `/` shows only centered "Hello World".

9. Verify: run the dev server, confirm `src/routeTree.gen.ts` is auto-generated by the plugin (never hand-edited or committed as a manual file), and confirm the page renders centered text with no console errors. Outcome: working ultra-minimal Hello World app.
